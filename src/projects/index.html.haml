---
layout: "../_layout/_main.html.haml"
class: "projects"
page_title: "Work"
...

%section.red-white
  .container
    .row
      .span10.offset1
        %h1 We make great digital products
    .row
      .span10.offset1
        %h2 We like to work together with smart people to make remarkable things we're proud of.

%section
  .container
    - projects.each_slice(2).to_a.each do |pair|
      .row
        - pair.each do |project|
          .span6.project
            %h2.mobile= project['name']
            %a{:href => href("/projects/#{project['name'].downcase}")}
              %img{:src => href("/images/work-#{project['name'].downcase}.jpg")}
              .hoover.desktop
                %h1= project['name']
                %h3= project['description']
